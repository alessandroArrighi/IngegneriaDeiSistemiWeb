- chiedere al prof se vada bene fare delle richieste di partenza generali al db per poi fare filtering più selettivo in locale
- chiedere funzionamento delle rotte in locale e sul server
- grafica la stessa dell'anno scorso, rimuoviamo i rettangoli, carosello standard senza bottoni, centrale a mo di vetrina
- homepage in stile vetrina, con occhiali in carosello e sotto scelta di configurazione/best seller/etc etc
- gestione personalizzazione e ordine del cliente: dati, carrello, checkout, pagamaneto

punti auth:
- modificare il secret con una variabile d'ambiente (variabile const JWT_SECRET = "foo")
- modificare l'alert di errore, (nel caso di errore con una chiama axios) nelle pagine vue, non è convenzione utilizzare i pop-up
